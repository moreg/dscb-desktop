/**
 * 开书 Phase 3（上）：卷级大纲 prompt（源自 story-long-write Phase 3 卷级大纲）。
 * 根据总章数计算推荐卷数，并生成大纲主文件（大纲.md）及卷纲文件。
 */

import { RHYTHM_FORMULA_SPEC, TOMATO_CHAPTER_NAME_SPEC } from './shared-specs'

export function buildVolumeOutlinePrompt(
  coreSettings: string,
  targetChapters: number,
  chapterWordCount: number
): string {
  // 根据公式计算推荐卷数：ceil(总章节数 / 30)，最少 6 卷，最多 50 卷
  const recommendedVolumes = Math.max(6, Math.ceil(targetChapters / 30))
  const avgChaptersPerVolume = Math.round(targetChapters / recommendedVolumes)

  const today = new Date().toISOString().slice(0, 10)

  return `## 任务：根据核心设定，产出全书卷级大纲（大纲.md）及所有卷的卷纲文件

### 📌 核心设定与背景
${coreSettings}

### 📏 篇幅与分卷约束
- **预计总章数**：${targetChapters} 章
- **每章字数**：约 ${chapterWordCount} 字
- **推荐卷数**：依据公式 \`ceil(总章节数 ÷ 30)\`（最少 6 卷，最多 50 卷），本书必须划分为 **${recommendedVolumes} 卷**。
- **每卷平均章节数**：约 **${avgChaptersPerVolume} 章**（单卷应处于 15-60 章之间，首尾和转折卷应有明显的起承转合结构）。
- **结构性卷约束**：
  - 第 1 卷必须为**起步卷**：完成开篇立人设、金手指觉醒，且第一个中/大爽点或小高潮在 10-15 章。
  - 中段必须包含**转折卷**（如卷 3 或卷 4）：核心真相揭示，力量体系或世界观出现重大转折。
  - 最后一卷（第 ${recommendedVolumes} 卷，即最终卷）必须为**收束卷**：完成所有主线收束、终极决战以及前文核心伏笔回收。
- **大纲四检（每卷设计必答）**：
  - 1. 本卷交付什么情绪？什么剧情模式能可靠交付？
  - 2. 本卷核心冲突是什么？
  - 3. 卷节奏（起承转合）哪段加速哪段减速？
  - 4. 本卷伏笔：需要新埋哪些伏笔？上一卷待回收的如何处理？

${RHYTHM_FORMULA_SPEC}

${TOMATO_CHAPTER_NAME_SPEC}

---

### 📐 格式规范（必须严格执行，违反会导致解析器静默丢数据）

1. **大纲.md 主文件格式**：
   - 标题格式规范：标题的 \`#\` \`##\` \`###\` 后面必须带有 exactly 一个空格（例如：\`# 《小说名》大纲\`、\`## 基本信息\`）。
   - 属性项必须为 \`- **字段**：值\` 形式，子列表缩进必须为 exactly 2 个空格。
   - 所有表格的第二行必须是 \`|---|...\` 分隔行。
   - **主线剧情走向** 部分下，每一卷的标题格式必须为：\`### 第X卷：卷名（第A-B章）\`（使用阿拉伯数字卷号，中文冒号，圆括号，如：\`### 第1卷：觉醒之日（第1-30章）\`）。
   - **关键转折点** 表格必须固定使用 5 列表格，列头必须准确包含关键字：\`编号\`、\`章节\`、\`类型\`、\`描述\`、\`状态\`。编号格式为 \`KP-NNN\`（如 KP-001）。类型枚举必须从以下范围选择：\`开篇钩子 / 身世揭示 / 金手指觉醒 / 境界突破 / 打脸升级 / 身份揭露 / 势力冲突 / 感情转折 / 真相揭露 / 卷终决战\`。章号必须为阿拉伯数字（如：\`第 8 章\`，不能用中文数字）。
   - **逐章节奏标注（与节奏图谱对齐）** 部分必须为每一卷创建独立的 H3 子节点（格式为：\`### 第X卷\`），并在其下输出表格。表格列头必须准确包含关键字：\`章节\`、\`标题\`、\`情绪值\`、\`爽点类型\`、\`卷\`。章号必须为阿拉伯数字格式（如 \`第 1 章\`，绝对禁止用中文数字）。爽点类型必须为数字数值：\`0\`（无爽点）、\`1\`（小打脸）、\`2\`（中打脸）、\`3\`（大高潮）、\`3.5\`（卷中决战）、\`4\`（卷终决战）。每一卷的末尾章节必须是爽点类型 \`4\`，情绪值 \`10\`，且每卷只能有 1 次卷终决战。连续无爽点（连续 2 章爽点类型为 0）属于严重违规，应予以避免。
   - **伏笔清单** 表格列头必须准确包含关键字：\`伏笔编号\`、\`伏笔内容\`、\`伏笔类型\`、\`埋设章节\`、\`预计回收章节\`、\`实际回收章节\`、\`状态\`。编号格式为 \`FB-NNN\`（如 FB-001）。类型枚举必须从以下范围选择：\`道具 / 身世 / 感情 / 设定 / 势力\`。状态枚举必须从以下选择：\`未回收 / 已埋设 / 已回收 / 已错过\`。
2. **各卷卷纲文件格式**：
   - 命名优先使用：\`大纲/卷纲_第X卷.md\`（如：\`大纲/卷纲_第1卷.md\`）。
   - 如你更自然地写成 \`大纲/卷纲_第一卷.md\` 这类中文数字卷名，系统也会兼容，但不要混用两套命名。
   - 文件顶部必须有版本头和修改记录。
   - 各章核心事件列表：每一章标题必须为 \`### 第X章：章节名\` 格式（如：\`### 第1章：困兽\`），内部必须带有核心事件、爽点、角色变动、伏笔这 4 个项。
3. **版本标记**：
   - 每个文件的最顶部必须包含版本和修改记录：
     \`\`\`markdown
     **版本**：v1.0（${today} 创建）
     **修改记录**：
     - v1.0（${today}）：初版
     \`\`\`

---

### 📤 输出要求

你需要合并输出大纲主文件（大纲.md）以及全部 ${recommendedVolumes} 个卷纲文件的内容。在每个文件内容前，使用 \`=== 相对路径 ===\` 进行标记（前后各 3 个等号，路径为半角斜杠），以便系统自动解析。请**仅输出**这 ${recommendedVolumes + 1} 个文件的内容，不要包含任何额外的解释或废话。

#### 输出结构示例：

=== 大纲/大纲.md ===
**版本**：v1.0（${today} 创建）
**修改记录**：
- v1.0（${today}）：初版

# 《书名》大纲

## 基本信息
- **题材**：{题材}
- **预计章节数**：${targetChapters} 章
- **每章字数**：约 ${chapterWordCount} 字
- **简介**：{简介}

## 力量/等级体系
- **境界划分**：{}

## 主线剧情走向

### 第1卷：{第 1 卷名}（第1-${avgChaptersPerVolume}章）
- **核心冲突**：{}
- **主角成长**：{}
- **爽点设计**：{}
- **伏笔铺设**：{}
- **卷末钩子**：{}

### 第2卷：{第 2 卷名}（第${avgChaptersPerVolume + 1}-${avgChaptersPerVolume * 2}章）
...

### 第${recommendedVolumes}卷：{第 ${recommendedVolumes} 卷名}（第${avgChaptersPerVolume * (recommendedVolumes - 1) + 1}-${targetChapters}章）
...

## 关键转折点

| 编号 | 章节 | 类型 | 描述 | 状态 |
|---|---|---|---|---|
| KP-001 | 第 1 章 | 金手指觉醒 | 主角激活金手指 | 已规划 |
| KP-002 | 第 ${avgChaptersPerVolume} 章 | 卷终决战 | 第一卷终极打脸 Boss 退场 | 已规划 |

## 逐章节奏标注（与节奏图谱对齐）

### 第1卷
| 章节 | 标题 | 情绪值 | 爽点类型 | 卷 |
|---|---|---|---|---|
| 第 1 章 | {标题} | 5 | 1 | 1 |
| 第 2 章 | {标题} | 6 | 1 | 1 |
...
| 第 ${avgChaptersPerVolume} 章 | {标题} | 10 | 4 | 1 |

### 第2卷
| 章节 | 标题 | 情绪值 | 爽点类型 | 卷 |
|---|---|---|---|---|
...

## 伏笔清单

| 伏笔编号 | 伏笔内容 | 伏笔类型 | 埋设章节 | 预计回收章节 | 实际回收章节 | 状态 |
|---|---|---|---|---|---|---|
| FB-001 | {伏笔描述} | 身世 | 第 1 章 | 第 ${avgChaptersPerVolume * 2} 章 | 未回收 | 已埋设 |

## 灵感来源记录
- 第 1 卷灵感：{}

=== 大纲/卷纲_第1卷.md ===
**版本**：v1.0（${today} 创建）
**修改记录**：
- v1.0（${today}）：初版

# 卷纲：第一卷 {第 1 卷名}（第1-${avgChaptersPerVolume}章）

## 卷核心
- **卷名**：{第 1 卷名}
- **章节范围**：第 1-${avgChaptersPerVolume} 章
- **核心冲突**：{}
- **核心情绪**：{压抑到爆发的曲线}
- **人物弧线**：{}

## 情绪弧线
{}

## 爽点节奏
{}

## 伏笔
- 铺设：FB-001
- 回收：无

## 反转
- 第 {X} 章：{}

## 各章核心事件

### 第 1 章：{第 1 章名}
- **核心事件**：{}
- **爽点**：{}
- **角色变动**：{}
- **伏笔**：{}

### 第 2 章：{第 2 章名}
...

=== 大纲/卷纲_第2卷.md ===
...

=== 大纲/卷纲_第${recommendedVolumes}卷.md ===
...

开始输出：`
}
